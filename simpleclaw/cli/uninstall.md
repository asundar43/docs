---
summary: "CLI reference for `simpleclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `simpleclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
simpleclaw uninstall
simpleclaw uninstall --all --yes
simpleclaw uninstall --dry-run
```
